package com.neo.signLanguage.data.models

class Color(colorName: String, colorValue: Int) {
    var colorName = ""
    var colorValue = 0

    init {
        this.colorName = colorName
        this.colorValue = colorValue
    }
}